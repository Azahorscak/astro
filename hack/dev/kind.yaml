# Demo cluster for astro development.
#
# One control plane and one worker is enough to exercise the deployment,
# namespace and binding rulesets while staying light on a laptop.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
